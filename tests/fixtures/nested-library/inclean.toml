[project]
root = "."
version = "0.2.0"

[[rule]]
name = "base"
paths = ["src/**"]
forms = ["quote"]
allowed_include_dirs = ["include"]
original_include_dirs = [
    "include/mylib/core",
    "include/mylib/core/detail",
]
