[project]
root = "."
version = "0.2.0"

[[rule]]
name = "a"
paths = ["src/**"]
forms = ["quote"]
action = { type = "keep" }

[[rule]]
name = "b"
paths = ["src/**"]
forms = ["quote"]
action = { type = "keep" }
