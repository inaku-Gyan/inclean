[project]
root = "."
version = "0.3.0-alpha.2"
min_inclean_version = "0.3.0-alpha.2"

[[rule]]
name = "rewrite"
file_paths = ["src/**/*"]
include_match = ["**"]
action = { type = "replace", with = "lib/${original}" }
