[project]
root = "."
version = "0.3.0-alpha.1"
min_inclean_version = "0.3.0-alpha.1"

# v1: matching a macro-form #include forces an Error regardless of the
# declared action. The file is skipped (no partial writes).

[[rule]]
name = "rewrite-quote"
file_paths = ["src/**/*"]
match_forms = ["quote"]
include_match = ["**"]
action = { type = "replace", with = "lib/${original}" }

[[rule]]
name = "rewrite-macro"
file_paths = ["src/**/*"]
match_forms = ["macro"]
include_match = ["**"]
action = { type = "keep" }
