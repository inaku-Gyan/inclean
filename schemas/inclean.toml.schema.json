{
  "$id": "https://raw.githubusercontent.com/inaku-Gyan/inclean/main/schemas/inclean.toml.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "inclean.toml",
  "description": "Configuration schema for inclean. Generate a documented starter config with `inclean init`.",
  "type": "object",
  "properties": {
    "project": {
      "$ref": "#/$defs/RawProject"
    },
    "rule": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/RawRule"
      },
      "minItems": 1
    }
  },
  "additionalProperties": false,
  "required": [
    "project"
  ],
  "$defs": {
    "RawProject": {
      "description": "The `[project]` block.",
      "type": "object",
      "properties": {
        "root": {
          "description": "Project root, relative to the config file's directory.\nDefaults to `\".\"` (the config's own directory).\nMust be a literal path, not a glob.",
          "type": "string",
          "default": "."
        },
        "version": {
          "description": "CLI version that wrote this config.",
          "type": "string"
        },
        "min_inclean_version": {
          "description": "Minimum CLI version that can parse this config.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "version",
        "min_inclean_version"
      ]
    },
    "RawRule": {
      "description": "A single `[[rule]]` entry. Raw rules deserialize exactly as written;\ninheritance from `copied_from`, `@std.*` constants, `${copied}`, and\neffective defaults are resolved later.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Globally unique rule name. Rules are evaluated in declaration order,\nand diagnostics refer to this name.",
          "type": "string"
        },
        "copied_from": {
          "description": "Name of a previously declared rule to copy from. The child starts from\nthe parent's already resolved value, so copy chains are transitive, but\nreferences are forward-only. Omitted top-level fields inherit from the\nparent; written top-level fields replace the parent's field. Inside a\nwritten object, omitted inner fields reset to their defaults unless the\nchild writes `${copied}` for that inner field.",
          "type": [
            "string",
            "null"
          ]
        },
        "file_paths": {
          "description": "Globset patterns matched against paths relative to `[project].root`.\nPatterns are ordered: a leading unescaped `!` negates, and the last\nmatching pattern wins. Use `\\!` for a literal leading `!`. `*` does not\ncross `/`; `**` does. Effective default for a rule with no parent is\n`[\"**/*\"]`. If a matching pattern contains wildcard characters,\n`file_suffixes` must also match; exact literal paths skip the suffix\ncheck.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "file_suffixes": {
          "description": "Literal extensions, including the leading dot, used after a wildcard\n`file_paths` match. Effective default for a rule with no parent is\n`[\"@std.c.extensions\", \"@std.cpp.extensions\"]`, expanded to the built-in\nC and C++ extension lists.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "suppression_comments_regex": {
          "description": "Optional per-rule suppression markers. Regexes are matched against each\nline after stripping `//` or same-line `/* */` delimiters when present\nand trimming whitespace. The whole field can also be the string\n`\"${copied}\"` to reuse the parent's resolved value verbatim.",
          "anyOf": [
            {
              "$ref": "#/$defs/RawSuppression"
            },
            {
              "type": "null"
            }
          ]
        },
        "include_forms": {
          "description": "Include delimiter forms this rule matches. Effective default for a rule\nwith no parent is `[\"quote\"]`. Header-like macro includes are\nstatically expanded when a unique simple `#define FOO \"bar.h\"` /\n`#define FOO <bar.h>` is found; otherwise `macro` can match the raw\n`#include FOO` token.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/IncludeForm"
          }
        },
        "macro_rewrite": {
          "description": "Where action rewrites for statically-expanded macro includes are written.\nEffective default for a rule with no parent is `\"definitions\"`, which\nedits the matching `#define` header token(s). `\"use_site\"` edits the\n`#include MACRO` use site only.",
          "anyOf": [
            {
              "$ref": "#/$defs/MacroRewrite"
            },
            {
              "type": "null"
            }
          ]
        },
        "include_match": {
          "description": "Globset patterns matched against the include argument with quotes or\nangle brackets stripped, for example `mylib/foo.h`. Patterns are\nordered: a leading unescaped `!` negates, and the last matching pattern\nwins. Use `\\!` for a literal leading `!`. `*` does not cross `/`; `**`\ndoes. Effective default for a rule with no parent is `[\"**\"]`.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "include_directories": {
          "description": "Literal directory paths under the project root (relative to it) that\nare probed after `include_forms` and `include_match` pass. NOT a glob;\nno implicit `/**` suffix; no `.gitignore` semantics. When this list is\nempty, no directory-resolution policy is applied.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "include_resolved_match": {
          "description": "Globset patterns matched against the resolved header path, relative to\nthe project root, after `include_directories` probes candidate files.\nPatterns are ordered like `include_match`. Effective default for a rule\nwith no parent is `[\"**\"]`. When `include_directories` is empty, this\nfield has no effect.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "include_on_unresolved": {
          "description": "Policy for a matched include when `include_directories` is non-empty\nand no resolved candidate remains after `include_resolved_match`\nfiltering. Effective default is `error`. `allow` keeps the rule\nmatched for non-`resolve` actions; it is rejected with action `resolve`.",
          "anyOf": [
            {
              "$ref": "#/$defs/IncludeOnUnresolved"
            },
            {
              "type": "null"
            }
          ]
        },
        "include_on_ambiguous": {
          "description": "Policy for a matched include when `include_directories` is non-empty\nand more than one directory contains the include argument. Effective\ndefault is `error`; `first` selects the first matching candidate in\ndeclaration order after `include_resolved_match` filtering.",
          "anyOf": [
            {
              "$ref": "#/$defs/IncludeOnAmbiguous"
            },
            {
              "type": "null"
            }
          ]
        },
        "action": {
          "description": "Action to run when all match layers pass. If neither this rule nor any\ncopied ancestor sets an action, the effective action is `\"skip\"`. The\nwhole field can also be the string `\"${copied}\"` to reuse the parent's\nresolved action, or `\"skip\"` to avoid contributing an action candidate.",
          "anyOf": [
            {
              "$ref": "#/$defs/RawAction"
            },
            {
              "type": "null"
            }
          ]
        },
        "trailing_comment": {
          "description": "Optional trailing-comment transform and/or append rule. If neither this\nrule nor any copied ancestor sets one, the effective value is `\"skip\"`.\nThe whole field can also be the string `\"${copied}\"` to reuse the\nparent's resolved value verbatim, or `\"skip\"` to avoid contributing a\ntrailing-comment candidate.",
          "anyOf": [
            {
              "$ref": "#/$defs/RawTrailingComment"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "RawSuppression": {
      "anyOf": [
        {
          "description": "Suppression markers: regex patterns matched line-by-line. `line` suppresses\nonly matching lines. `block_start` suppresses from its matching line until a\nlater `block_end` match, inclusive. If `block_end` is omitted, suppression\ncontinues to the end of the file after `block_start` matches.",
          "type": "object",
          "properties": {
            "block_start": {
              "description": "Regex that starts an off-limits block.",
              "type": [
                "string",
                "null"
              ]
            },
            "block_end": {
              "description": "Regex that ends an off-limits block.",
              "type": [
                "string",
                "null"
              ]
            },
            "line": {
              "description": "Regex that suppresses a single line.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Copy this whole object from the resolved parent rule named by copied_from.",
          "type": "string",
          "const": "${copied}"
        }
      ]
    },
    "IncludeForm": {
      "description": "Which \"form\" of `#include` a rule applies to.",
      "oneOf": [
        {
          "description": "`#include \"foo.h\"`",
          "type": "string",
          "const": "quote"
        },
        {
          "description": "`#include <foo.h>`",
          "type": "string",
          "const": "angle"
        },
        {
          "description": "`#include MY_HEADER` (macro-defined). Simple header-like macro\ndefinitions are expanded before rule matching when possible.",
          "type": "string",
          "const": "macro"
        }
      ]
    },
    "MacroRewrite": {
      "description": "Where a matched expanded macro include is rewritten.",
      "oneOf": [
        {
          "description": "Rewrite each matched header-like `#define` value.",
          "type": "string",
          "const": "definitions"
        },
        {
          "description": "Rewrite the `#include MACRO` use site.",
          "type": "string",
          "const": "use_site"
        }
      ]
    },
    "IncludeOnUnresolved": {
      "description": "Policy when `include_directories` is configured but no directory contains\nthe include argument.",
      "oneOf": [
        {
          "description": "Report an evaluation failure.",
          "type": "string",
          "const": "error"
        },
        {
          "description": "Treat this rule as not matched for this include.",
          "type": "string",
          "const": "skip"
        },
        {
          "description": "Keep this rule matched. Rejected for `resolve` actions.",
          "type": "string",
          "const": "allow"
        }
      ]
    },
    "IncludeOnAmbiguous": {
      "description": "Policy when `include_directories` is configured and multiple directories\ncontain the include argument.",
      "oneOf": [
        {
          "description": "Report an evaluation failure.",
          "type": "string",
          "const": "error"
        },
        {
          "description": "Treat this rule as not matched for this include.",
          "type": "string",
          "const": "skip"
        },
        {
          "description": "Select the first matching include directory in declaration order.",
          "type": "string",
          "const": "first"
        }
      ]
    },
    "RawAction": {
      "anyOf": [
        {
          "description": "The action a rule executes on a matched `#include`. This is a tagged object\nusing `type = \"resolve\"`, `\"replace\"`, `\"keep\"`, `\"remove\"`,\n`\"comment_out\"`, or `\"error\"`.",
          "oneOf": [
            {
              "description": "Rewrite the include using the header path selected by\n`include_directories`, relative to `relative_to`. `${current_file}`\nmeans the directory of the file being edited. Default `output_form`\nis `preserve`; default `message` is empty.",
              "type": "object",
              "properties": {
                "relative_to": {
                  "description": "Base path for the rewritten include. Use `${current_file}` for the\ncurrent source file's directory.",
                  "type": "string"
                },
                "output_form": {
                  "description": "Output delimiter form. Defaults to `preserve`.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/OutputForm"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "message": {
                  "description": "Optional diagnostic message string. `${current_file}` and\n`${original}` placeholders are supported where messages are emitted.",
                  "type": [
                    "string",
                    "null"
                  ],
                  "default": null
                },
                "type": {
                  "type": "string",
                  "const": "resolve"
                }
              },
              "additionalProperties": false,
              "required": [
                "type",
                "relative_to"
              ]
            },
            {
              "description": "Replace the include argument with `with`. Default `output_form` is\n`preserve`; default `message` is empty.",
              "type": "object",
              "properties": {
                "with": {
                  "description": "Replacement include argument. Supports `${original}` and\n`${current_file}` placeholders.",
                  "type": "string"
                },
                "output_form": {
                  "description": "Output delimiter form. Defaults to `preserve`.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/OutputForm"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "message": {
                  "description": "Optional diagnostic message string.",
                  "type": [
                    "string",
                    "null"
                  ],
                  "default": null
                },
                "type": {
                  "type": "string",
                  "const": "replace"
                }
              },
              "additionalProperties": false,
              "required": [
                "type",
                "with"
              ]
            },
            {
              "description": "Leave the include's argument alone (the form may still change via\n`output_form`). Default `output_form` is `preserve`; default `message`\nis empty.",
              "type": "object",
              "properties": {
                "output_form": {
                  "description": "Output delimiter form. Defaults to `preserve`.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/OutputForm"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "message": {
                  "description": "Optional diagnostic message string.",
                  "type": [
                    "string",
                    "null"
                  ],
                  "default": null
                },
                "type": {
                  "type": "string",
                  "const": "keep"
                }
              },
              "additionalProperties": false,
              "required": [
                "type"
              ]
            },
            {
              "description": "Delete the entire `#include` line. By default no blank line is kept and\na same-line trailing comment is kept on its own line.",
              "type": "object",
              "properties": {
                "keep_blank_line": {
                  "description": "Keep the line terminator as a blank line. Defaults to `false`.",
                  "type": [
                    "boolean",
                    "null"
                  ],
                  "default": null
                },
                "keep_trailing_comment": {
                  "description": "Preserve a recognized same-line trailing comment on its own line.\nDefaults to `true`.",
                  "type": [
                    "boolean",
                    "null"
                  ],
                  "default": null
                },
                "message": {
                  "description": "Optional diagnostic message string.",
                  "type": [
                    "string",
                    "null"
                  ],
                  "default": null
                },
                "type": {
                  "type": "string",
                  "const": "remove"
                }
              },
              "additionalProperties": false,
              "required": [
                "type"
              ]
            },
            {
              "description": "Wrap the include line in `//` (default) or `/* */` delimiters. Default\n`message` is empty.",
              "type": "object",
              "properties": {
                "style": {
                  "description": "Comment delimiter style. Defaults to `//`.",
                  "anyOf": [
                    {
                      "$ref": "#/$defs/CommentStyle"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "message": {
                  "description": "Optional diagnostic message string.",
                  "type": [
                    "string",
                    "null"
                  ],
                  "default": null
                },
                "type": {
                  "type": "string",
                  "const": "comment_out"
                }
              },
              "additionalProperties": false,
              "required": [
                "type"
              ]
            },
            {
              "description": "Report a user-facing error for the matched include. Exit code 2.",
              "type": "object",
              "properties": {
                "message": {
                  "description": "Error message. Supports `${original}` and `${current_file}`.\nDefaults to an empty string.",
                  "type": [
                    "string",
                    "null"
                  ],
                  "default": null
                },
                "type": {
                  "type": "string",
                  "const": "error"
                }
              },
              "additionalProperties": false,
              "required": [
                "type"
              ]
            }
          ]
        },
        {
          "description": "Copy this whole object from the resolved parent rule named by copied_from.",
          "type": "string",
          "const": "${copied}"
        },
        {
          "description": "Do not contribute this field to conflict detection for this rule.",
          "type": "string",
          "const": "skip"
        }
      ]
    },
    "OutputForm": {
      "description": "Output form for the include's delimiters after a rule fires.",
      "oneOf": [
        {
          "description": "`#include \"...\"`",
          "type": "string",
          "const": "quote"
        },
        {
          "description": "`#include <...>`",
          "type": "string",
          "const": "angle"
        },
        {
          "description": "Keep whatever form the original `#include` used.",
          "type": "string",
          "const": "preserve"
        }
      ]
    },
    "CommentStyle": {
      "description": "Trailing-comment delimiter style.",
      "oneOf": [
        {
          "description": "`// ...`",
          "type": "string",
          "const": "//"
        },
        {
          "description": "`/* ... */`",
          "type": "string",
          "const": "/**/"
        }
      ]
    },
    "RawTrailingComment": {
      "anyOf": [
        {
          "description": "Trailing-comment configuration for `resolve`, `replace`, and `keep`\nactions. Cross-line block comments after an include are not considered\ntrailing comments and are left alone.",
          "type": "object",
          "properties": {
            "transform": {
              "description": "Optional transform to run when an existing same-line trailing comment\nmatches.",
              "anyOf": [
                {
                  "$ref": "#/$defs/RawTrailingTransform"
                },
                {
                  "type": "null"
                }
              ]
            },
            "append_if_absent": {
              "description": "Literal text to append to the include line when there is no\ntrailing comment after action evaluation. The user writes the full\ncomment text, including delimiters and leading whitespace. It must not\ncontain line terminators.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Copy this whole object from the resolved parent rule named by copied_from.",
          "type": "string",
          "const": "${copied}"
        },
        {
          "description": "Do not contribute this field to conflict detection for this rule.",
          "type": "string",
          "const": "skip"
        }
      ]
    },
    "RawTrailingTransform": {
      "description": "Trailing-comment transform: matches an existing comment, then runs an\naction over it. `action` is required so a transform cannot silently become\na no-op because of an omitted nested action.",
      "type": "object",
      "properties": {
        "match_styles": {
          "description": "Existing comment styles that can match. Effective default is both\n`//` and `/**/`.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/CommentStyle"
          }
        },
        "content_regex": {
          "description": "Regex matched against the trimmed trailing-comment body. Effective\ndefault is `.*`.",
          "type": [
            "string",
            "null"
          ]
        },
        "action": {
          "description": "Action to run when style and regex both match.",
          "$ref": "#/$defs/RawTrailingAction"
        }
      },
      "additionalProperties": false,
      "required": [
        "action"
      ]
    },
    "RawTrailingAction": {
      "description": "The action a trailing-comment transform runs on its match.",
      "oneOf": [
        {
          "description": "Replace the comment body with `with`. Default `output_style` is\n`preserve`; default `message` is empty.",
          "type": "object",
          "properties": {
            "with": {
              "description": "Replacement comment body. Supports `${original}` and\n`${current_file}` placeholders.",
              "type": "string"
            },
            "output_style": {
              "description": "Output comment delimiter style. Defaults to `preserve`.",
              "anyOf": [
                {
                  "$ref": "#/$defs/OutputCommentStyle"
                },
                {
                  "type": "null"
                }
              ]
            },
            "message": {
              "description": "Optional diagnostic message string.",
              "type": [
                "string",
                "null"
              ],
              "default": null
            },
            "type": {
              "type": "string",
              "const": "replace"
            }
          },
          "additionalProperties": false,
          "required": [
            "type",
            "with"
          ]
        },
        {
          "description": "Keep the comment body; only `output_style` may change. Default\n`output_style` is `preserve`; default `message` is empty.",
          "type": "object",
          "properties": {
            "output_style": {
              "description": "Output comment delimiter style. Defaults to `preserve`.",
              "anyOf": [
                {
                  "$ref": "#/$defs/OutputCommentStyle"
                },
                {
                  "type": "null"
                }
              ]
            },
            "message": {
              "description": "Optional diagnostic message string.",
              "type": [
                "string",
                "null"
              ],
              "default": null
            },
            "type": {
              "type": "string",
              "const": "keep"
            }
          },
          "additionalProperties": false,
          "required": [
            "type"
          ]
        },
        {
          "description": "Remove the trailing comment entirely. Default `message` is empty.",
          "type": "object",
          "properties": {
            "message": {
              "description": "Optional diagnostic message string.",
              "type": [
                "string",
                "null"
              ],
              "default": null
            },
            "type": {
              "type": "string",
              "const": "remove"
            }
          },
          "additionalProperties": false,
          "required": [
            "type"
          ]
        },
        {
          "description": "Report a user-facing error when the transform matches.",
          "type": "object",
          "properties": {
            "message": {
              "description": "Error message. Supports `${original}` for the original comment body\nand `${current_file}` for the edited source path.",
              "type": [
                "string",
                "null"
              ],
              "default": null
            },
            "type": {
              "type": "string",
              "const": "error"
            }
          },
          "additionalProperties": false,
          "required": [
            "type"
          ]
        }
      ]
    },
    "OutputCommentStyle": {
      "description": "Trailing-comment output style with an extra `preserve` variant for\n`trailing_comment.transform.action.output_style`.",
      "oneOf": [
        {
          "description": "`// ...`",
          "type": "string",
          "const": "//"
        },
        {
          "description": "`/* ... */`",
          "type": "string",
          "const": "/**/"
        },
        {
          "description": "Keep whatever delimiter the original trailing comment used.",
          "type": "string",
          "const": "preserve"
        }
      ]
    }
  }
}
